im// Les composants de debug et test ont été retirés car nous utilisons maintenant MySQLrt React from "react";
import HomeSections from "../../components/home/HomeSections";
import SectionDebugger from "../../components/debug/SectionDebugger";
import TestApiConnection from "../../components/test/TestApiConnection";


export default function HomePage() {
  return (
    <div className="relative bg-gray-50 dark:bg-slate-900/90 transition-colors duration-300">

      <div 
        className="flex flex-col min-h-screen overflow-y-auto overflow-x-hidden snap-y snap-mandatory scroll-smooth"
        style={{ 
          scrollbarWidth: 'none', 
          msOverflowStyle: 'none',
          WebkitOverflowScrolling: 'touch',
          scrollSnapType: 'y mandatory',
          scrollBehavior: 'smooth'
        }}
      >
        {/* Système dynamique - gère automatiquement toutes les sections */}
        <HomeSections />
      </div>
      
      {/* Debug components (dev only) */}
      {process.env.NODE_ENV === 'development' && <SectionDebugger />}
      {process.env.NODE_ENV === 'development' && <TestApiConnection />}
    </div>
  );
}