/**
 * ⚠️ SERVICES MOCK COMPLÈTEMENT SUPPRIMÉS
 * 
 * Ce fichier ne contient plus aucune fonctionnalité mock.
 * Tous les services utilisent maintenant UNIQUEMENT l'API backend.
 * 
 * Si vous voyez des erreurs d'import, vérifiez les fichiers suivants :
 * - authService.js (pour l'authentification)
 * - orderService.js (pour les commandes)
 * - productService.js (pour les produits et collections)
 * - firebaseService.js (pour les fonctions Firebase simulées)
 * - homeService.js (pour les sections de la page d'accueil)
 */

console.error('⚠️ ERREUR: mockServices.js ne doit plus être importé!');
console.error('🔄 Utilisez les services API appropriés à la place:');
console.error('   - authService.js pour l\'authentification');
console.error('   - orderService.js pour les commandes');
console.error('   - productService.js pour les produits');
console.error('   - firebaseService.js pour les fonctions Firebase');
console.error('   - homeService.js pour la page d\'accueil');

// Ne plus exporter quoi que ce soit d'utile
export default null;
